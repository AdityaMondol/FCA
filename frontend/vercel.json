{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "sveltekit",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://your-backend-url.render.com/$1"
    }
  ]
}